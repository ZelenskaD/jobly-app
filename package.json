{
  "name": "jobly-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix jobly\"",
    "heroku-postbuild": "npm install --prefix jobly && npm run build --prefix jobly"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}

